apiVersion: v1
kind: Secret
metadata:
  name: fleet-pipeline-pull-secret
  namespace: openshift-pipelines
type: kubernetes.io/dockerconfigjson
stringData:
  .dockerconfigjson: |
    {
      "auths": {
        "quay.io": {
          "auth": "REPLACE_WITH_BASE64_CREDENTIALS"
        }
      }
    }
