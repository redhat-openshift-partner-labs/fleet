apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: apply-ai-workloads
spec:
  params:
    - name: cluster-name
      type: string
    - name: pipeline-image
      type: string
    - name: source-dir
      type: string
  workspaces:
    - name: source
  steps:
    - name: apply
      image: $(params.pipeline-image)
      imagePullPolicy: IfNotPresent
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        fleet-apply-ai-workloads \
          --cluster-name "$(params.cluster-name)" \
          --source-dir "$(params.source-dir)" \
          --spoke-kubeconfig "$(workspaces.source.path)/kubeconfig"