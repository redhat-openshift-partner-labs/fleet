apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: finalize-spoke
spec:
  params:
    - name: cluster-name
      type: string
    - name: pipeline-image
      type: string
  workspaces:
    - name: shared
  steps:
    - name: finalize
      image: $(params.pipeline-image)
      imagePullPolicy: IfNotPresent
      script: |
        #!/usr/bin/env bash
        set -euo pipefail
        fleet-finalize-spoke \
          --cluster-name "$(params.cluster-name)" \
          --spoke-kubeconfig "$(workspaces.shared.path)/kubeconfig"
